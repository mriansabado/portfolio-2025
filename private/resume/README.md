## Resume Storage

The original resume PDF is stored locally in this folder as `Ian_Sabado_Resume_Master.pdf`.

It is intentionally kept out of the public app bundle and ignored by git via `.gitignore` so the website can use summarized content without exposing the source resume file directly.

Site-facing summary content now lives in `src/data/resume.ts`.
